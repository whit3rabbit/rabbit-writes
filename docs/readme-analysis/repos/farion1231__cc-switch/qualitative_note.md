# farion1231/cc-switch — qualitative note

This README is dominated structurally by an enormous collapsible sponsor table (`<details>`/`<summary>` wrapping ~20 rows of API-relay-service ads with affiliate links) that sits above the actual "Why CC Switch?" product explanation — a notable anti-pattern in that a first-time visitor has to scroll past a wall of paid promotions before reaching any substantive content, even though the collapse control at least keeps it from dominating visually. Once past that, the README becomes tightly structured and genuinely useful: a centered header block with multi-language README links (English/中文/日本語/Deutsch), bold-labeled bullet features ("One App, Eight Tools," "No More Manual Editing"), and an unusually thorough FAQ built from real support pain points (plugin config disappearing after switching, Wayland/NVIDIA click bugs with an exact environment-variable workaround) rather than generic Q&A filler. Deeper technical content — architecture diagram, dev commands, project structure tree — is cleanly tucked into collapsible `<details>` sections, keeping the main scroll short while still making the full depth discoverable. The FAQ's specificity (e.g., naming the exact hidden database path and backup rotation count) is a strong example of grounding claims in concrete, checkable facts rather than vague reassurance.

**Quotes:**
- "CC Switch follows a 'minimal intrusion' design principle — even if you uninstall the app, your CLI tools will continue to work normally."
- "The AppImage forces `GDK_BACKEND=x11` (XWayland) to avoid a historical native-Wayland crash. On newer Wayland + NVIDIA setups this can leave the web content area unclickable..."
