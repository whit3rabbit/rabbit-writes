# demo-drift

Fixture for the changelog tells. The code-span mention of `used to` below stays quiet.

## History that should be standing facts

- We fixed the flaky teardown in the socket tests.
- The parser used to accept trailing commas.
- The old exporter is no longer part of the build.
- Previously the cache was cleared on every request.
- The quote handling was corrected, fixed in commit abc1234f.
